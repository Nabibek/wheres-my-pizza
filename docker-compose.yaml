version: "3.9"

services:
  postgres:
    image: postgres:15
    container_name: restaurant_postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: restaurant_system  # Add this to create the database
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql  # Add this line
    networks:
      - restaurant_net

  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbitmq
    ports:
      - "5672:5672"   # AMQP
      - "15672:15672" # Management UI
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
  # application:
  #   build: 
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: app
  #   environment:
  #     DATABASE_URL: postgres://postgres:postgres@postgres:5432/orders_db?sslmode=disable
  #     RABBITMQ_URL: amqp://guest:guest@rabbitmq:5672/
  #   depends_on:
  #     - postgres
  #     - rabbitmq
  #   ports:
  #     - "3000:3000"
  #   networks:
  #     - restaurant_net

volumes:
  postgres_data:

networks:
  restaurant_net:
    driver: bridge



